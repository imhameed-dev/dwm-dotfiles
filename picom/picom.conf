# Minimal compositor: only tear-free rendering. No shadows, fading, blur or rounded corners.
backend = "xrender";
vsync = true;
use-damage = true;
unredir-if-possible = true;
shadow = false;
fading = false;
